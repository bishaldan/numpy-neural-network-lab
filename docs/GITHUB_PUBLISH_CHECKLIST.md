# GitHub Publish Checklist

- Confirm `docker compose run --rm test` passes
- Confirm `docker compose run --rm trainer` produces fresh artifacts
- Confirm `docker compose run --rm experiments` writes comparison outputs
- Add 2-3 demo screenshots to `docs/assets/`
- Keep README metrics and commands aligned with the current repo
- Review `outputs/mnist_run` so it contains representative sample artifacts only
- Add a repository description and topics on GitHub
- Pin the repo on your profile after publishing
